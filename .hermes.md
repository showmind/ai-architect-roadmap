# .hermes.md — Hermes 子代理调度规则

> 本文件是 Hermes 调度子代理（MiMo Code / OpenCode / CodeBuddy 等）开发本项目时的行为准则。
> 每次派发任务前，子代理**必须**遵守本文件。

## 1. 角色定位

- **Hermes 是唯一的调度中心和最终审阅者**。
- **子代理（MiMo/OpenCode/CodeBuddy）只负责执行**：按 KU 模板开发完整交付物。
- **Hermes 不做业务代码编写**，只做：派发 → 验收 → 合并 → 更新看板。

## 2. 任务派发规则

### 2.1 任务粒度
- 一个任务 = **一个知识单元（KU）**。
- 每个 KU 交付物见 `MASTER_PLAN.md` 第三节。

### 2.2 工具选择
- 默认使用 **MiMo Code**（小米限时免费通道，零 API 费）。
- 如 MiMo 不可用，fallback 到 **OpenCode**（BYO key）。
- 并行任务可分发不同工具提速。

### 2.3 任务描述模板
```
任务：开发知识单元 {KU编号} — {KU标题}

路径：units/{level}/{ku-id}/

要求：
1. 按 MASTER_PLAN.md 第三节的载体设计规范交付完整目录
2. 主页面 index.html 需包含互动演示（Canvas/SVG）
3. 动画页 animation.*.html 用 p5.js 或 CSS Animation 实现
4. quiz.html 至少 3 道互动题（单选/多选/拖拽）
5. diagram.svg 用 architecture-diagram skill 规范
6. meta.json 含标题/难度/标签/引用源

技术约束：
- 纯前端（HTML/CSS/JS），无后端依赖
- 静态资源内联或放 assets/，确保离线可运行
- 代码需注释，引用源写在 meta.json 中

验收标准：
- 浏览器打开 index.html 可正常交互
- 动画流畅，题目可作答并判分
- 代码无 console 报错
```

## 3. 代码质量标准

- HTML 语义化、可访问性（a11y）
- CSS 变量主题化（方便后续深色模式）
- JS 模块化（ES Modules 或 IIFE）
- 中文 UI、注释
- 引用的博客/书籍必须在 meta.json 中署名

## 4. GitHub 协作规则

- 每个 KU 一个分支：`feat/{KU编号}-{slug}`
- 一个 PR = 一个 KU
- PR 描述模板：
  ```
  ## 知识单元：{KU编号} {标题}
  ### 交付物
  - [ ] index.html（主互动页）
  - [ ] animation.*.html（动画）
  - [ ] diagram.svg（架构图）
  - [ ] quiz.html（互动题）
  - [ ] meta.json（元信息）
  ### 引用源
  - ...
  ```
- Hermes 审阅通过后合并到 main

## 5. 通信协议

- Hermes → 子代理：通过 terminal 命令派发
- 子代理 → Hermes：通过文件变更 + 输出日志
- 子代理**不能**自行 push、自行合并

## 6. 紧急约定

- 如果子代理卡住超过 5 分钟，Hermes kill 并 retry
- 如果免费模型额度耗尽，自动切换备用工具
- 遇到不可解决的问题，子代理在输出中标记 `ESCALATE`，Hermes 升级处理
